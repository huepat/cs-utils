namespace HuePat.Util.Object.Properties {
    public enum PropertyType {
        BOOL,
        BYTE,
        INTEGER,
        LONG,
        FLOAT,
        DOUBLE,
        VECTOR3D,
        COLOR
    }
}